import React from 'react';
import {Button} from '@flavorli/elements';

const SaveRecipe = () => {
  return (
    <Button intent="secondary" width="90px">
      Add to
    </Button>
  );
};

export default SaveRecipe;
