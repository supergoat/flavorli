import {styled} from '@flavorli/elements';

const Root = styled.div`
  width: ${props => `${props.theme.screens.XS}px`};
  height: 700px;
  overflow: auto;
  background: #fff;
`;

export default Root;
